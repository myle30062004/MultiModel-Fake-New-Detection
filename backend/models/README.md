Place the trained model artifacts in this folder:

- `phobert_text_best/` containing the PhoBERT tokenizer and model files
- `multimodal_model.pth` containing the trained fusion checkpoint

The backend expects these assets to exist before startup.
